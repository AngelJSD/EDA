#include "node.h"

template<typename T, typename C>
class cVPT<T, C>{
	public:
		cNode<T>* mRoot;

		cVPT(){
			mRoot == nullptr;
		};

		int Distance(string, string);
		int Distance(mus, mus);

		void Generation(T files, C){

		};

};
